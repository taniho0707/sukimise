# Production Environment Configuration for Sukimise
# Copy this file to .env.production and update the values

# Database Configuration
DATABASE_URL=postgres://sukimise_user:CHANGE_THIS_PASSWORD@postgres:5432/sukimise?sslmode=disable
POSTGRES_DB=sukimise
POSTGRES_USER=sukimise_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_DATABASE_PASSWORD

# JWT Secret (CRITICAL: Change this to a strong random value)
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_JWT_SECRET_AT_LEAST_32_CHARS

# Port Configuration for Production
# Main external access port (http://HOST_DOMAIN_NAME:PORT/)
PORT=80                    # Frontend nginx server (main external access point)

# Discord Bot port (external access)
BOT_PORT=8082             # Discord Bot port (independent external access)

# Internal backend port (not exposed externally)
BACKEND_PORT=8080         # Backend API port (internal only, accessed via /api/ proxy)

# CORS Configuration (CRITICAL: Set your production domains)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOW_CREDENTIALS=true

# Discord Bot Configuration
DISCORD_TOKEN=YOUR_PRODUCTION_DISCORD_BOT_TOKEN
SUKIMISE_API_URL=http://backend:8080

# Google Maps API
GOOGLE_MAPS_API_KEY=YOUR_PRODUCTION_GOOGLE_MAPS_API_KEY

# User Management (Generate with scripts/hash_password.go)
# Format: username1:bcrypt_hash1;username2:bcrypt_hash2
ADMIN_USERS=admin:$2a$10$GENERATE_PROPER_BCRYPT_HASH_FOR_ADMIN
EDITOR_USERS=editor:$2a$10$GENERATE_PROPER_BCRYPT_HASH_FOR_EDITOR

# Production Security Notes:
# 1. Change all default passwords and secrets
# 2. Use strong, randomly generated JWT_SECRET (32+ characters)
# 3. Set proper CORS_ALLOWED_ORIGINS for your domain
# 4. Generate proper bcrypt hashes for user passwords
# 5. Use environment-specific Discord and Google Maps credentials
# 6. Consider using secrets management system (Docker Secrets, Vault, etc.)

# Optional: For reverse proxy setup
# NGINX_PORT=80
# NGINX_SSL_PORT=443

# Optional: SSL Configuration
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/private.key